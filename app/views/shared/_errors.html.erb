<div class="containter">
<% if @mArticle.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h4>Following errors prevented the article from being saved</h4>
      <ul>
        <% @mArticle.errors.full_messages.each do | msg | %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
